# Secrets
.env

# Runtime data — generated by the bot, not source. Never gitignore the *code* that
# produces these; only the output.
state.json
state_live*.json
config_overrides.json
journal/
logs/
backtest/
dashboard/data.js
dashboard/backtests/
halt.flag

# cTrader OAuth tokens (access/refresh) -- generated by ops/ctrader_auth.py, never commit.
ctrader_tokens.json

# Python
__pycache__/
*.pyc
venv/
.venv/
.pytest_cache/

# OS
Thumbs.db
.DS_Store
